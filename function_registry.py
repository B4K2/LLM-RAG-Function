FUNCTION_METADATA = {
    "open_application": {
        "description": "Opens an application based on the given name.",
        "example": "open_application('chrome')"
    },
    "run_command": {
        "description": "Executes a shell command in the terminal.",
        "example": "run_command('ls -la')"
    },
    "get_system_info": {
        "description": "Fetches system resource usage like CPU, RAM, or disk usage.",
        "example": "get_system_info('cpu')"
    }
}
